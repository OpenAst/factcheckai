services:
  api:
    build:
      context: .
      target: web
    environment:
      PORT: 8000
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      CACHE_DB_PATH: /app/storage/cache.db
      REDIS_URL: redis://redis:6379/0
      OCR_QUEUE_NAME: ocr_jobs
      OCR_JOB_TTL_SECONDS: 1800
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
      SERPER_API_KEY: ${SERPER_API_KEY}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
      RELIABLE_NEWS_DOMAINS: ${RELIABLE_NEWS_DOMAINS:-}
      GUIDANCE_PDF_PATHS: ${GUIDANCE_PDF_PATHS:-}
    depends_on:
      redis:
        condition: service_started
    expose:
      - "8000"
    volumes:
      - api-data:/app/storage
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5)"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped

  worker:
    build:
      context: .
      target: worker
    environment:
      REDIS_URL: redis://redis:6379/0
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      OCR_QUEUE_NAME: ocr_jobs
      OCR_JOB_TTL_SECONDS: 1800
      EASYOCR_LANGS: en
      OCR_MAX_IMAGE_DIMENSION: 1600
      OCR_DOWNLOAD_MODELS: "true"
      EASYOCR_MODEL_DIR: /app/storage/easyocr
    depends_on:
      redis:
        condition: service_started
    volumes:
      - worker-models:/app/storage/easyocr
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  api-data:
  worker-models:
  redis-data:
